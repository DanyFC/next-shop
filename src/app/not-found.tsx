import { NotFoundPage } from "@/components";

export default function NotFound() {
  return (
    <div className="min-h-screen flex justify-center items-center">
      <NotFoundPage message="This page does not exists!!" />
    </div>
  );
}